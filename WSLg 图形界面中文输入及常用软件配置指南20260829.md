

本文档总结了在 WSL2 (Ubuntu) 环境下，解决图形界面（GUI）软件无法输入中文的问题，以及如何安装 Google Chrome 浏览器。

## 一、 配置 Linux 内部中文输入法 (Fcitx5)

WSLg 目前无法直接透传 Windows 的中文输入法状态，必须在 Linux 环境内部单独安装并配置输入法。推荐使用 **Fcitx5**。

### 1. 安装中文字体
防止软件界面中文显示为方块（豆腐块）：
```bash
sudo apt update
sudo apt install fonts-noto-cjk fonts-wqy-microhei
```

### 2. 安装 Fcitx5 及相关组件
```bash
sudo apt install fcitx5 fcitx5-chinese-addons fcitx5-frontend-gtk3 fcitx5-frontend-qt5 dbus-x11
```

### 3. 配置输入法环境变量
将以下环境变量写入 shell 配置文件（如 `~/.bashrc` 或 `~/.zshrc`）：
```bash
cat << 'EOF' >> ~/.bashrc

# Fcitx5 Input Method Variables
export GTK_IM_MODULE=fcitx
export QT_IM_MODULE=fcitx
export XMODIFIERS=@im=fcitx
export SDL_IM_MODULE=fcitx
export GLFW_IM_MODULE=ibus
EOF
```
使环境变量生效：
```bash
source ~/.bashrc
```

### 4. 启动 Fcitx5
在启动图形界面软件前，需确保 Fcitx5 守护进程在后台运行：
```bash
fcitx5 -d
```

### 5. 在图形界面中添加拼音输入法
启动配置工具：
```bash
fcitx5-configtool
```
- 取消勾选右侧的“Only Show Current Language”。
- 搜索 `pinyin` 并将其添加到左侧的“当前输入法”列表中。
- 应用并关闭。之后在 Linux 图形软件中按 `Ctrl + Space` 即可切换中文。

---

## 二、 常见错误排查

### 报错：`inputcontextmanager.cpp:318] All display connections are gone, exit now.`
该错误表明 Fcitx5 无法连接到图形显示服务器（通常是因为 Wayland 兼容性或显示服务器未唤醒）。

**解决方法 1：临时禁用 Wayland 插件（推荐）**
```bash
fcitx5 --disable=wayland -d
```
*若成功，可进入 `fcitx5-configtool` -> 附加组件 (Addons) -> 高级选项，永久禁用 Wayland 插件。*

**解决方法 2：使用 DBus 启动**
```bash
dbus-launch fcitx5 --disable=wayland -d
```

**解决方法 3：唤醒 WSLg 服务**
先随便运行一个图形软件（如 `fcitx5-configtool &`），再执行 `fcitx5 -d`。

**终极排查**
检查环境变量 `echo $DISPLAY`，若无输出（正常应为 `:0`），需在 Windows 中执行 `wsl --shutdown` 重启子系统。

---

## 三、 安装 Google Chrome 浏览器

通过终端使用 `wget` 和 `apt` 进行安装。

### 1. 下载与安装
```bash
# 下载官方安装包
wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb

# 自动处理依赖并安装
sudo apt install ./google-chrome-stable_current_amd64.deb

# 清理安装包
rm google-chrome-stable_current_amd64.deb
```

### 2. 启动 Chrome
正常启动：
```bash
google-chrome
```
如果遇到崩溃或沙盒 (Sandbox) 错误，请添加禁用沙盒参数启动：
```bash
google-chrome --no-sandbox
```