
## 背景说明

ROS2 默认使用 Fast DDS 作为中间件，该方案在 WSL2 虚拟网络环境下的兼容性较差。建议在 WSL2 中切换至 **Cyclone DDS**，其对复杂网络环境的自适应能力更强，是 ROS 社区在 WSL2 中最普遍的做法。

## 步骤一：安装 Cyclone DDS

将 `<distro>` 替换为你正在使用的 ROS2 版本号（如 `humble`、`iron`、`jazzy` 等）：

```bash
sudo apt update
sudo apt install ros-<distro>-rmw-cyclonedds-cpp
```

## 步骤二：切换环境变量

在每个终端窗口中分别执行：

```bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
```

> ⚠️ **注意**：该环境变量需要在每个新的终端窗口中单独设置，或在步骤三中配置为永久生效。

## 步骤三：（可选）永久生效

将环境变量写入 `~/.bashrc`，使每次打开终端自动生效：

```bash
echo "export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp" >> ~/.bashrc
source ~/.bashrc
```

## 常见问题：`turtle_teleop_key` 键盘无响应

使用 `turtle_teleop_key` 节点时，键盘控制失效的常见原因是**终端焦点丢失**。

- **原因**：该节点通过捕获终端的标准输入流（stdin）来读取按键指令。若鼠标点击了海龟动画窗口，终端将失去焦点，按键无法被接收。
- **解决**：操作时请确保**鼠标焦点停留在运行键盘控制程序的那个终端窗口上**，而非海龟动画窗口。
